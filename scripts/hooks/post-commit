#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
python3 "$ROOT_DIR/scripts/review_agent_safety_check.py" || {
  echo "[post-commit] review-agent-safety check failed." >&2
  exit 1
}
